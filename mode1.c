#include "mode1.h"
#include "geral.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void find_error(char **lines, int lines_n, char **dict, int size_dict, char *out_file){
    // check if each word in line is valid+in dict/within offset of word in dict
    // for wrong words detected chose based on out_file == NULL to print or to write mistakes
    if (out_file == NULL){ // No output file (print to terminal)

    }
    else{ // With output file

    }
}